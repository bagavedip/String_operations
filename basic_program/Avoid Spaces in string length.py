# program to avoid spaces in string
example = " Dipika bagave"
result = example.strip(" ")
print(result)


# python program to avoid spaces in string
examples = " hello World"
result = "".join(examples)
print(len(result))
print(result)
